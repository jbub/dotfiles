dotfiles
========

My dotfiles managed by python dotfiles.

```bash
cd $HOME && git clone git@github.com:jbub/dotfiles.git Dotfiles
```

List

```bash
dotfiles --list
```

Check

```bash
dotfiles --check
```

Sync

```bash
dotfiles --sync --force
```
